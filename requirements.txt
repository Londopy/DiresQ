Flask==3.1.0

# Windows-only wheel, no sdist. Installs locally, breaks ubuntu-latest in CI.
pygeospy==0.2.0

# parse() raises ParseError on no match, and returns Instant | Range | Ambiguous.
# Wrap every call.
timefuzz==0.3.1
