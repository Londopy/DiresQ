Flask==3.1.0

# Windows-only wheel, no sdist. The marker keeps `pip install -r` working on
# Linux CI, which would otherwise die here before running a single check.
# Anything importing this needs a non-Windows fallback.
pygeospy==0.2.0; sys_platform == "win32"

# parse() raises ParseError on no match, and returns Instant | Range | Ambiguous.
# Wrap every call.
timefuzz==0.3.1
